{
  "name": "federated-graphql-v1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compose": "rover supergraph compose --config ./supergraph-config.yaml",
    "compose:build": "rover supergraph compose --config ./supergraph-config.yaml > ./supergraph.graphql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "^2.6.2",
    "@apollo/subgraph": "^2.6.2",
    "apollo-server": "^3.13.0"
  }
}
